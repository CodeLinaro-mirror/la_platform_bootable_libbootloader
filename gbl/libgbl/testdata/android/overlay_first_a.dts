/dts-v1/;
/plugin/;

/ {
    // add a new node at the root level
    fragment@0 {
        target-path = "/chosen";
        __overlay__ {
            // Add the node with property to this sub-node
            first_overlay {
                overlay_a_property = "overlay_a_val";
            };
        };
    };
};
